import { VoiceConsole } from "./voice-console";

export const dynamic = "force-dynamic";

/** Ezer s'utilise à la voix : la console est la surface d'entrée. */
export default function Home() {
  return <VoiceConsole />;
}
